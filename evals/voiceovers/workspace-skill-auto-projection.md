# workspace-skill-auto-projection — every new local workspace receives current Skills

1. I create a new local workspace and return it only after its engine-specific Skill files are present. Existing trusted built-in projections also advance to the current package version without another manual sync.
