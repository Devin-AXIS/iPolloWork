1. This video conversation recovers files from its session directory regardless of model. Only the current session's main index file is shown; supporting CSS, JSON, images, and unrelated HTML are omitted from the generated-file cards.

2. Implementation assets remain available to the video runtime without cluttering the conversation or creating misleading Design actions.

3. Selecting the active index file opens the current conversation's Video Studio, preserving the one-project-per-session contract.
