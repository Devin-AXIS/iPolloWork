1. This video conversation recovers files from its session directory regardless of model. Only the current session's main index file is presented as an action; supporting CSS and unrelated HTML remain visible without an open control.

2. Selecting supporting CSS or unrelated HTML does not create a Design tab or replace the active right-side editor.

3. Selecting the active index file opens the current conversation's Video Studio, preserving the one-project-per-session contract.
