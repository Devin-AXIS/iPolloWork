# Video 组件数据契约

验证数据驱动的视频组件使用同一套对用户友好、对 AI 可读的数据结构，同时保持 HyperFrames 原生时间线和组件源码不变。

1. 美国地图不再要求用户输入难懂的逗号字符串，而是显示地区和数值组成的结构化表单。每一行都有类型、校验和清晰字段，AI 也读取同一份语义契约。

2. 修改加州数值后，当前地图实例立即保存新数据并继续出现在 HyperFrames 时间线上；可复用的地图源码没有被改写，其他实例仍可独立使用。
