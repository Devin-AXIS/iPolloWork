{
  "name": "douyin-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.22.0" },
  "scripts": {
    "start": "node service/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check service/api.mjs && node --check service/store.mjs && node --check service/operations.mjs && node --check service/server.mjs && node --check service/workbench.mjs && node --check ui/app.js"
  }
}
