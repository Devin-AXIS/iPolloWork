---
name: browser-setup
description: Use the built-in iPolloWalk browser
---

Try browser automation in iPolloWalk right away.

IMPORTANT:
- Use the built-in iPolloWalk browser tools.
- In your first response, open `https://example.com` and tell the user the page title.
- If browser tools are unavailable, tell the user browser control is not available in this workspace and ask them to retry after iPolloWalk reloads.

Keep the response short and action-oriented.
